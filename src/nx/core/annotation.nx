namespace Core
  # Would embed an exported function into the binary,
  # built for given *Arch*. Functions built for the same
  # architecture are grouped together in a single module.
  @[ApplicableTo<"export:function">]
  annotation Embed<
    Arch : %string,
    SubArch : %string,
    Vendor : %string,
    System : %string,
    ABI : %string>
  end
end
