require "./object"

# Primitives are objects which don't allow instance variables
# and usually have a number of `native` methods.
#
# This module is implicitly derived by all `primitive`s.
# NOTE: It can not be derived explicitly.
#
# ```
# @assert({{ Int32 < Primitive }})
# ```
#
module Primitive
  derive Object
end
