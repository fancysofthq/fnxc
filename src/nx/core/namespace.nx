# This module is implicitly derived by all namespaces.
# NOTE: It can not be derived explicitly.
#
# A namespace can have variables, constants
# and function declarations. Namespace declarations
# don't allow `static` modifiers, because they
# already are somewhat implicitly static.
#
# ```
# namespace Foo
# end
#
# def main
#   @assert({{ Foo < Namespace }})
# end
# ```
module Namespace
end
