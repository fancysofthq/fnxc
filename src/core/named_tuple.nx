struct NamedTuple(**T)
  \{% for k, v in T:pairs() do %}
    protected var \{{ k }} : \{{ v }}
  \{% end %}

  static def new(named_tuple : **T)
    return named_tuple
  end

  threadsafe const def size
    \{{ T:size() }}
  end
end
