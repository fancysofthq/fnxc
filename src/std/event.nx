# OS      | Event mechanism
# ---     | ---
# FreeBSD | kqueue
# MacOS   | kqueue
# Windows | IOCP
# Linux   | epoll
#
# > epoll notifies when a file descriptor is ready to perform
# > an I/O operation, whereas kqueue & IOCP notify when
# > a requested operation has completed.
struct Event
  static def
end
